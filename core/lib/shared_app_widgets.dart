import 'package:flutter/cupertino.dart';

class Empty extends StatelessWidget {
  const Empty({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => SizedBox(
      height: 0,
      width: 0,
    );
}
